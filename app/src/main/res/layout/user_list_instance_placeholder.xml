<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="15dp"
    android:paddingVertical="8dp"
    tools:background="@color/activity_background">

    <View
        android:id="@+id/shimmer_image"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="@drawable/shimmer_text_shape"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/rocky" />

    <TextView
        android:id="@+id/shimmer_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:background="@drawable/shimmer_text_shape"
        android:maxLines="1"
        android:textSize="20sp"
        app:layout_constraintLeft_toRightOf="@id/shimmer_image"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="A$AP ROCKY" />

    <TextView
        android:id="@+id/shimmer_mail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:background="@drawable/shimmer_text_shape"
        android:maxLines="1"
        android:textSize="12sp"
        app:layout_constraintHorizontal_bias="0"
        app:layout_constraintLeft_toLeftOf="@id/shimmer_name"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/shimmer_name"
        app:layout_constraintWidth_percent="0.7"
        tools:text="asap.rocky@music.com" />

</androidx.constraintlayout.widget.ConstraintLayout>