<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/topic_name"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginEnd="5dp"
        android:background="@drawable/topic_choose_item_box_shape"
        android:ellipsize="end"
        android:fontFamily="@font/inter_regular"
        android:gravity="center_vertical"
        android:maxLines="1"
        android:paddingStart="26dp"
        android:paddingEnd="64dp"
        android:textColor="#99FFFFFF"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/choose_indicator"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="@id/choose_indicator"
        app:layout_constraintTop_toTopOf="@id/choose_indicator"
        tools:text="Topic name" />

    <View
        android:id="@+id/choose_indicator"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/topic_chooser_nipple"
        android:elevation="2dp"
        android:outlineProvider="background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>